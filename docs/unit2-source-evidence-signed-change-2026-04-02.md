# Unit 2 Source Evidence: Signed Change (2026-04-02)

## Status

- This note isolates the Unit 2 signed addition and subtraction cluster:
  - `U2-S4` add and subtract integers and rational numbers
- This note is strong enough to support `provisional` app-facing skill status
  for `U2-S4` in the current conservative tutor loop.
- This note does not approve multiplicative signed-expression work, final unit
  completion claims, or numeric mastery thresholds.

## Source Basis

- `C:\MathFile\중1\중1_ 미래엔 중단원마무리_1_2_정수와_유리수.pdf`
- `C:\MathFile\중1\[수준별 문제은행_발전] Ⅰ-2. 정수와 유리수.pdf`

## Extracted Evidence

### Contextual signed-change conditions

- 발전 문제은행 page 1 includes a problem of the form:
  - adding one negative quantity makes the result negative
  - adding a different negative quantity makes the result positive
  - the learner must determine which integer values are possible
- This supports the current tutor framing that `U2-S4` is not just arithmetic
  execution, but also direction/sign reasoning under conditions.

### Number-line movement and expression translation

- 미래엔 중단원마무리 page 2 includes a problem asking which calculation
  expression is represented by a number line.
- The same cluster supports translating between symbolic signed addition and
  left-right movement on the number line.
- This is the direct source basis for the current app wording that asks the
  learner to read sign as directed change before calculating.

### Direct signed computation checks

- 미래엔 중단원마무리 page 3 includes items asking which signed computation
  result is incorrect.
- These items keep the target on sign consistency through calculation, not only
  on category language or absolute-value interpretation.
- Together with the contextual and number-line items above, this supports
  treating `U2-S4` as a distinct skill cluster inside Unit 2.

## What This Slice Approves

- `docs/mastery-rules.md` may use `U2-S4` as a worked example for conservative
  `ready_for_next_step` evidence in Unit 2.
- `app/content/unit2-scaffold/skills.json` may mark `U2-S4` as `provisional`
  when that record points back to this note and `docs/mastery-rules.md`.

## What This Slice Does Not Approve

- `U2-S5` multiplication, division, reciprocal, and power promotion
- new prerequisite links beyond the current `draft-from-docs` subset
- recommendation ranking changes
- final mastery thresholds or pass counts

## Next Slice

1. Extract a separate source-backed note for `U2-S5` multiplicative signed
   expressions.
2. After the operation cluster is complete, decide whether Unit 2 activity,
   recommendation, lesson-step, rubric, and transcript records can be promoted
   beyond `draft-from-docs`.
